#include<iostream>
using namespace std;
int main(){
    // int x;
    // x=5>8 ? 10 : 1!=2<5 ? 20 : 30;
    // cout<<"Value of x : "<<x;
    char x; 
int a = 2;
x = (a > 0) ? 'a' : 'S';
cout << "Value of x : " << x << endl; 
cout << "Value of a : "<< a;
    return 0;
}